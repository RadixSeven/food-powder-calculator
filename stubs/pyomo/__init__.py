"""Local pyomo type-stub package marker.

This namespace exists so pyrefly can pick up :mod:`pyomo.environ` from
:mod:`stubs.pyomo.environ` — pyomo itself ships untyped, so we maintain
the protocols we need under ``stubs/`` and add it to ``search-path`` in
``pyproject.toml``.
"""
